<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Color x:Key="ButtonDangerNormalColor">#FFC42B1C</Color>
    <Color x:Key="ButtonDangerHoverColor">#FFE04F3F</Color>
    <SolidColorBrush x:Key="ButtonDangerNormalBrush" Color="{StaticResource ButtonDangerNormalColor}"/>
    <SolidColorBrush x:Key="ButtonDangerHoverBrush" Color="{StaticResource ButtonDangerHoverColor}"/>

    <Style TargetType="{x:Type ui:Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="24,8" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:Button}">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <Grid>
                            <Border x:Name="HoverBorder" 
                                    Background="Transparent" 
                                    Opacity="0" 
                                    CornerRadius="3" />
                                    
                            <StackPanel x:Name="ContentPanel"
                                        Orientation="Horizontal"
                                        Margin="{TemplateBinding Padding}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                
                                <ui:SymbolIcon x:Name="LeftIcon" 
                                               Symbol="{TemplateBinding Icon}" 
                                               Visibility="Collapsed" />
                                               
                                <ContentPresenter x:Name="ContentPresenter" 
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- In Wpf UI, Appearance="Primary" maps to Accent Filled -->
                        <Trigger Property="Appearance" Value="Primary">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
                            <Setter TargetName="HoverBorder" Property="Background" Value="{DynamicResource SystemAccentColorSecondaryBrush}" />
                        </Trigger>
                        <!-- Appearance="Secondary" maps to Outlined -->
                        <Trigger Property="Appearance" Value="Secondary">
                            <Setter TargetName="RootBorder" Property="BorderThickness" Value="1" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
                            <Setter TargetName="HoverBorder" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                        </Trigger>
                        <!-- Appearance="Danger" -->
                        <Trigger Property="Appearance" Value="Danger">
                            <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource ButtonDangerNormalBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
                            <Setter TargetName="HoverBorder" Property="Background" Value="{StaticResource ButtonDangerHoverBrush}" />
                        </Trigger>

                        <!-- Icon Visibility (WPF UI usually has Icon property) -->
                        <Trigger Property="Icon" Value="Empty">
                            <Setter TargetName="LeftIcon" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <!-- Fallback: if not empty, assume there's an icon. 
                             Usually WPF UI button icons are set explicitly. -->

                        <!-- Hover Animations for all Appearances -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBorder"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1"
                                                         Duration="0:0:0.22">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard FillBehavior="Stop">
                                        <DoubleAnimation Storyboard.TargetName="HoverBorder"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.28">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.85" />
                            <Setter TargetName="ContentPanel" Property="Opacity" Value="0.75" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentPanel" Property="Opacity" Value="0.2" />
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.55"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
