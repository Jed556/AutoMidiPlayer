<UserControl x:Class="AutoMidiPlayer.WPF.Dialogs.AppStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:s="https://github.com/canton7/Stylet">
    
    <Grid Width="280" VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <ui:SymbolIcon Symbol="{Binding StatusIcon}" 
                           FontSize="34" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,12"/>

            <TextBlock Text="{Binding StatusTitle}"
                       FontWeight="Bold"
                       FontSize="24"
                       TextAlignment="Center"
                       Margin="0,0,0,8"/>

            <!-- For Reset Mode -->
            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="13"
                       Opacity="0.6"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Visibility="{Binding IsUpdateStatus, Converter={x:Static s:BoolToVisibilityConverter.InverseInstance}}"/>

            <!-- For Update Mode -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        Opacity="0.6"
                        Visibility="{Binding IsUpdateStatus, Converter={x:Static s:BoolToVisibilityConverter.Instance}}">
                <TextBlock Text="{Binding OldVersion}"
                           FontSize="13"
                           VerticalAlignment="Center"/>
                <ui:SymbolIcon Symbol="TriangleRight20"
                               FontSize="9"
                               Filled="True"
                               VerticalAlignment="Center"
                               Margin="8,1,8,0"/>
                <TextBlock Text="{Binding NewVersion}"
                           FontSize="13"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
