namespace AutoMidiPlayer.WPF.Controls.Snackbar;

/// <summary>
/// Specifies where the snackbar container positions its items within the content area.
/// </summary>
public enum SnackbarPlacement
{
    TopLeft,
    TopCenter,
    TopRight,
    BottomLeft,
    BottomCenter,
    BottomRight
}
