<UserControl x:Class="AutoMidiPlayer.WPF.Controls.Section"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <StackPanel>
        <Grid Visibility="{Binding HasHeader, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Margin"
                            Value="0,0,0,8"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasDescription, ElementName=Root}"
                                     Value="True">
                            <Setter Property="Margin"
                                    Value="0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="{Binding Title, ElementName=Root}"
                       FontSize="{Binding TitleFontSize, ElementName=Root}"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>

            <ContentPresenter Grid.Column="1"
                              Margin="12,0,0,0"
                              VerticalAlignment="Center"
                              Content="{Binding HeaderContent, ElementName=Root}"
                              Visibility="{Binding HasHeaderContent, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>

        <TextBlock Text="{Binding Description, ElementName=Root}"
                   FontSize="12"
                   Opacity="0.6"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"
                   Visibility="{Binding HasDescription, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <ContentPresenter Margin="0,8,0,0"
                Content="{Binding ChildContent, ElementName=Root}"/>
    </StackPanel>
</UserControl>
